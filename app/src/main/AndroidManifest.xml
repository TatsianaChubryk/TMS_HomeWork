<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homework">

    <application
        android:name=".hw.homework.lesson26.PatientsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeWork">
        <activity android:name=".hw.homework.lesson26.PatientsActivity" />
        <activity android:name=".hw.homework.lesson26.NewPatientsActivity" />

        <service
            android:name=".hw.homework.lesson21.MediaPlayerService"
            android:enabled="true"
            android:exported="true"></service>

        <activity android:name=".hw.homework.lesson22.TaskFragment"></activity>
        <activity android:name=".hw.homework.lesson21.MediaPlayerFragment" />
          <activity android:name=".hw.homework.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>