<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.homework">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeWork">
        <activity android:name=".hw.homework.lesson.TaskSixActivity"></activity>
        <activity android:name=".hw.homework.lesson.TaskFiveActivity" />
        <activity android:name=".hw.homework.lesson.TaskFourActivity" />
        <activity android:name=".hw.homework.lesson.TaskThreeActivity" />
        <activity android:name=".hw.homework.lesson.TaskTwoActivity" />
        <activity android:name=".hw.homework.lesson.TaskOneActivity" />
        <activity android:name=".hw.homework.lesson22.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>